enum MessageType {
  error,
  success,
  warning,
}

enum AuthenticationStatus {
  unknown,
  onBoarding,
  authenticated,
  unAuthenticated,
}

enum AppBottomNavItemStatus {
  dashboard,
  createTodo,
  profile,
}
